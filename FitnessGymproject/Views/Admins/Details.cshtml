@model FitnessGymproject.Models.Admin

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<div class="row">
    <div class="col-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Admin Details</h4>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Field</th>
                                <th>Details</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>Full Name</td>
                                <td>@Html.DisplayFor(model => model.FullName)</td>
                            </tr>
                            <tr>
                                <td>Email</td>
                                <td>@Html.DisplayFor(model => model.Email)</td>
                            </tr>
                            <tr>
                                <td>Password</td>
                                <td>@Html.DisplayFor(model => model.Password)</td>
                            </tr>
                            <tr>
                                <td>Created At</td>
                                <td>@Html.DisplayFor(model => model.CreatedAt)</td>
                            </tr>
                            <tr>
                                <td>Updated At</td>
                                <td>@Html.DisplayFor(model => model.UpdatedAt)</td>
                            </tr>
                            <tr>
                                <td>Gender</td>
                                <td>@Html.DisplayFor(model => model.Gender)</td>
                            </tr>
                            <tr>
                                <td>Profile Image</td>
                                <td>
                                    @if (!string.IsNullOrEmpty(Model?.Imageprofileurl))
                                    {
                                        <img src="@Model.Imageprofileurl" alt="Profile Image" class="img-thumbnail" width="100" height="100" />
                                    }
                                    else
                                    {
                                        <span>No Image Available</span>
                                    }
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model?.AdminId" class="btn btn-warning">Edit</a>
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
</div>
